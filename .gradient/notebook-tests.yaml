
direct-s3-access:
  location: ./
  generated: true
  description: Run the symlinking
  cmd: python3 /notebooks/.gradient/symlink_datasets_and_caches.py --num-concurrent-downloads {conc_down} --max-concurrency {max_concurrency} --no-symlink
  parameters:
    conc_down: "8,10,15,20,40"
    max_concurrency: "1,4"

